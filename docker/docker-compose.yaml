services:
  ocr_service:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8081:8080"
    networks:
      - symbiota-network
    env_file:
      - ../transcription/.env

networks:
  symbiota-network:
    external: true